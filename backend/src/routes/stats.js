// DEPRECATED — superseded by /api/players/:address (stats embedded in profile)
// and /api/house/summary. Kept as an empty stub for git history; safe to delete.
import { Router } from 'express';
export default Router();
