# Resolve peer-dep conflicts the way npm@6 did (warn, don't error).
# We rely on this for the @ton/core <-> @ton/ton peer constraint, and for any
# transitively-installed package that uses strict peer ranges (express plugins,
# etc.). Remove this line once the rest of the dep tree is on @ton/core >=0.62.
legacy-peer-deps=true
